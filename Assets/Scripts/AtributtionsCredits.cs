using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class AtributtionsCredits : MonoBehaviour
{
    private string[] names = new string[5];
    void Start()
    {
        
        names[0] = "babysofja";//freepik
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
